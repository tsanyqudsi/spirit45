import { Typography } from '@mui/material';
import { Row } from '@components';

export const Shoutout = (): JSX.Element => {
  return (
    <Row id={'shoutout'} marginY={'4rem'}>
      <Typography variant='h2'>Spirit45 will be fun </Typography>
    </Row>
  );
};
